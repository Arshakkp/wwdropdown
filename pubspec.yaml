name: wwdropdown
description:  customizable dropdown with API pagination and search. Supports lazy loading, debounced API calls, and dynamic filtering for efficient handling of large datasets. Fully customizable UI and behaviors
version: 1.0.2
repository: https://github.com/Arshakkp/wwdropdown

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

license: MIT